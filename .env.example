# Copy this file to `.env` and fill in the values before running the bot or backend services.

# --- Telegram bot configuration ---
TELEGRAM_TOKEN=your-telegram-bot-token
TELEGRAM_PAYMENT_SECRET= # optional: secret to validate Telegram payment callbacks
SECRET_KEY= # optional: Flask secret key for backend session signing

# --- Administrative access ---
ADMIN_LOGIN_PASSWORD=change-me
ROOT_ADMIN_ID= # numeric Telegram ID of the root admin
ADMINS= # optional: comma-separated Telegram IDs of admins with panel access
ADMIN_ROOT_IDS= # optional: comma-separated Telegram IDs with root privileges

# --- Database configuration ---
# Alembic понимает любые из этих переменных и автоматически конвертирует
# async-драйвер (postgresql+asyncpg) в psycopg2
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/postgres
# Append ?sslmode=require when connecting to managed hosts (Render/Railway)
DATABASE_URL_SYNC=postgresql+psycopg2://postgres:postgres@localhost:5432/postgres?sslmode=require
DB_URL= # optional: legacy alias used by Alembic migrations

# --- Backend security & integrations ---
BACKEND_HMAC_SECRET=backend-shared-secret
BACKEND_IDEMPOTENCY_TTL=3600
ROBLOX_API_BASE_URL= # optional: override Roblox API endpoint

# --- TON / Wallet Pay configuration ---
WALLET_PAY_API_BASE=https://pay.wallet.tg
WALLET_PAY_API_KEY=
WALLET_PAY_SHOP_ID=
TON_PAYMENT_MARKUP_PERCENT=0
TON_INVOICE_TTL_SECONDS=900

# --- Webhook & hosting settings ---
DOMAIN= # public domain used for webhook callbacks
WEBHOOK_PATH=/webhook
WEBHOOK_URL= # optional: explicit webhook URL override
PORT=10000 # optional: override Render-provided port when running locally
SERVICE_ROLE=bot # optional: choose "bot" or "backend" when using render_start.sh locally